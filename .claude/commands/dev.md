---
description: Start the datastar-tutor dev server in the background
allowed-tools: Bash(npm run dev)
---

Run `npm run dev` in the background (it's a long-running server via `node --watch`, so use `run_in_background: true`). Do not ask for confirmation first — this command is pre-approved. After starting it, report the URL it's listening on (check server.js for the port, default http://localhost:3000).
